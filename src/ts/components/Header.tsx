import React  from 'react';

const Header = (): JSX.Element => {

    return (
        <div className="header">
            <div className="logo" >
                carb<b>non</b>
            </div>
        </div>
    );
};

export default Header;